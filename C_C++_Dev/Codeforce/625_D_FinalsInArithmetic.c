#include "stdio.h"
#include "string.h"

int i,j,len;
char s[100005];
char ans[100005];

int recur(int x, int y, int isZ, int isP){
    int i,j;
    for(i = 0; i <= 9; i++){
        for(j = 0; j < i; j++){
            if(i+j==0)
        }
    }
}

int main(){
    scanf(" %s",s);
    len = strlen(s);
    recur(0,len-1,0,0);

}
