#include "stdio.h"

int castle[100][100];

int findLowest(int x, int y, int t){

}

int main(){
    int m,n;
    int a,b,t;

    for(fcount = 0; fcount < 1; fcount++){
        scanf("%d%d", &m, &n);
        for(i = 0; i < m; i++){
            for(j = 0; j < n; j++){
                scanf("%d", &castle[i][j]);
            }
        }
        scanf("%d%d%d", &a, &b, &t);
        findLowest(0,0,0)
    }
}
